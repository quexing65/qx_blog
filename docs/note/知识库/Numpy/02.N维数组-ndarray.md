---
tags:
  - Python
  - Numpy
  - 数据分析
date: 2024-09-01
updated: 2026-07-31
---

# 二、N维数组-ndarray

## 学习目标

- 目标
  - 说明数组的属性，形状、类型

---

## 1 ndarray的属性

数组属性反映了数组本身固有的信息。

|     属性名字     |          属性解释          |
| :--------------: | :------------------------: |
|  ndarray.shape   |       数组维度的元组       |
|   ndarray.ndim   |          数组维数          |
|   ndarray.size   |      数组中的元素数量      |
| ndarray.itemsize | 一个数组元素的长度（字节） |
|  ndarray.dtype   |       数组元素的类型       |

## 2 ndarray的形状

首先创建一些数组。

```python
# 创建不同形状的数组
>>> a = np.array([[1,2,3],[4,5,6]])
>>> b = np.array([1,2,3,4])
>>> c = np.array([[[1,2,3],[4,5,6]],[[1,2,3],[4,5,6]]])
```

分别打印出形状

```python
>>> a.shape
>>> b.shape
>>> c.shape

(2, 3)  # 二维数组
(4,)	# 一维数组
(2, 2, 3) # 三维数组
```

如何理解数组的形状？

二维数组：

![数组1](assets/%E6%95%B0%E7%BB%841.png)

三维数组：

![数组2](assets/%E6%95%B0%E7%BB%842.png)



## 3 ndarray的类型

```python
isinstance(score.dtype, np.dtype)  # True
```

dtype是numpy.dtype类型，先看看对于数组来说都有哪些类型

|     名称      |                          描述                           | 简写  |
| :-----------: | :-----------------------------------------------------: | :---: |
|   np.bool_    |         布尔类型（True 或 False）         |  '?'  |
|    np.int8    |            tinyint一个字节大小，-128 至 127             |  'i'  |
|   np.int16    |              smallint整数，-32768 至 32767              | 'i2'  |
|   np.int32    |                32 位有符号整数，-2^31 至 2^31 - 1                | 'i4'  |
|   np.int64    |              bigint整数，-2^63 至 2^63 - 1              | 'i8'  |
|   np.uint8    |          tinyint unsigned无符号整数，0 至 255           |  'u'  |
|   np.uint16   |         smallint unsigned无符号整数，0 至 65535         | 'u2'  |
|   np.uint32   |                无符号整数，0 至 2^32 - 1                | 'u4'  |
|   np.uint64   |                无符号整数，0 至 2^64 - 1                | 'u8'  |
|  np.float16   |    半精度浮点数：16位，正负号1位，指数5位，精度10位     | 'f2'  |
|  np.float32   |  float单精度浮点数：32位，正负号1位，指数8位，精度23位  | 'f4'  |
|  np.float64   | double双精度浮点数：64位，正负号1位，指数11位，精度52位 | 'f8'  |
| np.complex64  |        复数，分别用两个32位浮点数表示实部和虚部         | 'c8'  |
| np.complex128 |        复数，分别用两个64位浮点数表示实部和虚部         | 'c16' |
|  np.object_   |                       python对象                        |  'O'  |
|  np.bytes_    |                  定长字节串                          |  'S'  |
|  np.str_      |                  定长 Unicode 字符串                  |  'U'  |

常用的几个：

**`np.int32`**：32位整数，是最常用的整数类型，适用于大多数整数运算。

**`np.float64`**：64位浮点数，是默认的浮点数类型，广泛用于科学计算。

**`np.bool_`**：布尔类型，用于表示True或False，常用于条件判断和逻辑操作。

**`np.bytes_` / `np.str_`**：分别表示定长字节串和定长 Unicode 字符串。NumPy 2.0 已移除 `np.string_` 与 `np.unicode_` 别名。

**`np.object_`**：用于存储任意Python对象，特别是在处理混合类型数据或需要灵活性的时候。

> `np.bytes_` 保存原始字节，不负责文本编码；需要文本时通常使用 Python 字符串、`np.str_`，或在表格数据中使用 pandas/Arrow 的字符串 dtype。定长字符串会截断超过容量的值，创建时应明确长度和编码边界。

**创建数组的时候指定类型**

```python
>>> a = np.array([[1, 2, 3],[4, 5, 6]], dtype=np.float32)
>>> a.dtype
dtype('float32')

>>> arr = np.array([b'python', b'tensorflow', b'scikit-learn', b'numpy'], dtype=np.bytes_)
>>> arr
array([b'python', b'tensorflow', b'scikit-learn', b'numpy'], dtype='|S12')
```

- 注意：默认整数 dtype 取决于平台和 NumPy 版本，不应无条件假设为 `int64`；默认 Python 浮点数通常推断为 `float64`。对文件格式、网络协议和跨平台数据应显式指定 dtype 与字节序。

## 4 总结

数组的基本属性【知道】

|     属性名字      |          属性解释          |
| :---------------: | :------------------------: |
| **ndarray.shape** |       数组维度的元组       |
|   ndarray.ndim    |          数组维数          |
|   ndarray.size    |      数组中的元素数量      |
| ndarray.itemsize  | 一个数组元素的长度（字节） |
| **ndarray.dtype** |       数组元素的类型       |

[上一步：01.Numpy优势](01.Numpy优势.md) [下一步：03.基本操作](03.基本操作.md) [返回总索引](关于Numpy.md)
