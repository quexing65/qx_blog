# 更新日志

记录博客的重要改动。最新在最上。

## 2026-08-18

### 修复：CI drift 校验误报——中文文件夹排序的跨平台差异

- 文件：`scripts/update-sidebar.js`
- 现象：CI 上线首跑即红。本机生成的侧边栏文件夹按拼音序（电脑→墙外→一些），
  CI 的 Linux 环境重新生成时按部首序（一些→墙外→电脑），两边不一致触发 drift 报警
- 原因：`localeCompare` 不带 locale 参数时跟随系统默认 locale，Windows 中文环境和
  Linux 英文环境对中文的排序规则不同
- 修复：文件夹排序显式指定 `zh-Hans-CN`（Node 自带完整 ICU，任意平台结果一致）；
  同日期文章排序只比较 ASCII 日期串，无此问题
- 意外之喜：drift 校验第一次运行就抓到一个真实的跨平台 bug，验证了这条校验的价值

### 新增：GitHub Actions CI——质量保障不再依赖本机

- 文件：`.github/workflows/ci.yml`、`scripts/check-links.js`、`package.json`
- 背景：所有检查（跑 `npm run update`、pre-commit 钩子、链接可用性）都依赖本机记得执行，
  且 pre-commit 钩子不随 clone 走，换电脑即失效；2026-08-17 的 CDN 静默失效正是这样漏掉的
- CI 两个独立 job（push main / PR 触发）：
  - 同步检查：`npm run check` → `npm run update` → `git diff --exit-code docs/`。
    生成文件与提交版本不一致（忘了跑 update / 文章缺 front-matter 被 CI 补写）即报红
  - 链接检查：`npm run check:links`，新增脚本逐个请求 index.html 里的外链
    （HEAD 优先、405/501 回退 GET、失败重试一次防 CDN 限流假红），
    并校验内部引用（favicon、theme.css、darkmode.js）文件存在；HTML 注释里的链接不检查
- 本地验证：20 个外链 + 3 个内部引用全部通过；`npm run update` 后无 drift

### 新增：文章页动态标题

- 文件：`docs/index.html`
- 新增 `updateTitlePlugin`（doneEach）：渲染完成后把当前文章名写进 `document.title`
  （如「win e · quexing 的博客」），命名口径与侧边栏脚本一致（去 .md、下划线转空格）
- 浏览器标签 / 历史记录 / 分享链接从此能区分文章；首页保持原标题

### 修复：update-sidebar 的 BOM 误判与日期时区偏移

- 文件：`scripts/update-sidebar.js`、`docs/index.html`
- BOM：Windows 记事本保存的文件带 UTF-8 BOM 头，`^---` 匹配不到 front-matter，
  会误判后重复补写；现读入时剥离 BOM（回写时顺手清除），前端剥离插件同步容忍。
  已用真实 BOM 文件实测：补写正常、BOM 清除、正文完好（当前仓库无带 BOM 文件，纯预防）
- 时区：`formatDate` 原用 `toISOString()`（UTC），北京时间 0-8 点创建的文章会被记成
  前一天；改用本地时区取年月日

## 2026-08-17

### 修复：三处 CDN 链接损坏，代码高亮 / mermaid / 点击特效静默失效

- 文件：`docs/index.html`
- 原因：7a31f44 批量替换协议相对 URL 时，把 mermaid、shiki、click-heart 三个本来就是
  `https://` 的地址错误改成了 `https:https://` 前缀（该提交信息声称修复此问题，实际是引入）。
  浏览器把 `https:https://cdn...` 解析为主机名 `https` 的地址，必然加载失败
- 影响：所有代码块无语法高亮（降级纯文本）、mermaid 图表不渲染、红心点击特效消失。
  因当时只有 archive 目录的文章含代码块，一直未被发现
- 修复：三处改回 `https://`，并在归档文章页验证高亮与复制按钮恢复

### 修复：.gitignore 笔误

- `.qpder/` → `.qoder/`，Qoder IDE 生成目录此前未被忽略

### 优化：文章日期改为 front-matter 优先，摆脱对文件 mtime 的依赖

- 文件：`scripts/update-sidebar.js`、`docs/index.html`、四篇文章
- 背景：脚本原先用文件 mtime 做首页日期与排序，但 git 不保留 mtime——
  换电脑后三篇文章的 mtime 全部变成拷贝日 2026-07-25，再跑脚本会覆盖真实日期
- 方案：文章头部写 `--- date: YYYY-MM-DD ---`，脚本优先读取；没有 front-matter 的新文章退回
  mtime。日期跟着内容走，换电脑 / 重新 clone / 修错别字都不再影响
- `index.html` 新增 `stripFrontMatterPlugin`（beforeEach），front-matter 块不会渲染成正文
- 已补写现有四篇文章的真实日期：宝可梦 2026-08-02、win e 2026-07-07、别回头 2026-07-03、
  魔法推荐 2026-06-17；重跑脚本后首页与手工修正结果一致

### 优化：update-sidebar 支持 updated 字段与 front-matter 自动补写

- 文件：`scripts/update-sidebar.js`、`docs/note/墙外的世界/宝可梦每月优惠口令.md`
- `updated` 字段：front-matter 可选第二行 `updated: YYYY-MM-DD` 表示最后内容更新日；
  排序按 updated（无则按 date），首页对这类文章显示「发布于 X · 更新于 Y」
- 自动补写：扫描到没有 front-matter 的文章时，用当前 mtime 把 `date:` 写进文件头部并保存，
  日期第一次生成即被锁定——忘记写 front-matter 不再有后果
  （前提：写完文章后尽快跑一次脚本，隔太久 mtime 就不准了）
- 宝可梦口令篇改为 date: 2026-07-01 + updated: 2026-08-02，如实反映「每月更新」的性质；
  其余三篇维持单一 date
- 已用临时文章实测自动补写流程（补写 → 生成 → 删除 → 恢复）通过

### 清理：删除 update-sidebar.js 死分支与 theme.css 死样式

- 文件：`scripts/update-sidebar.js`、`docs/css/theme.css`（对应优化清单「代码清理」项）
- 删除 renderList 的 showTime 参数与 HTML 渲染分支（从未被调用，旧版侧边栏设计遗留）
- 删除仅由该分支产出的 `.article-folder` / `.article-item` 样式，以及全仓库零引用的
  `.article-card` 系列样式（更早期设计遗留），theme.css 净减约 40 行
- 验证：重跑脚本后 `_sidebar.md` / `home.md` 与删除前逐字节一致

### 优化：SEO 基础两项 + PNG 批量压缩

- `<title>` 花体 Unicode「𝓺𝓾𝓮𝔁𝓲𝓷𝓰」→「quexing 的博客」：浏览器标签页与搜索引擎可正常匹配，
  页面内 hero / 侧边栏的花体视觉不受影响（那是渲染出来的文字，不依赖 title）
- `<meta name="description">` 占位符 "Description" →「记录与分享 · 技术 · 生活」
- `docs/assets/` 全部 13 张 PNG 用 sharp 调色板量化（quality 90）批量压缩：
  3.84MB → 0.84MB（-78%），保持 PNG 格式与文件名不变，文章引用零改动；
  已验证文章页 10 张图片正常加载
- 对应优化清单 SEO 前两条与性能条目已勾选

### 优化：OG 分享卡片

- `docs/index.html` 新增 5 个 OG 标签（title / description / image / type / url），
  微信、QQ、X 等平台粘贴链接时显示预览卡片
- 新增 `docs/_media/og-card.png`（1200×630）：极简奶油底 + 站点吉祥物（icon.svg 的绿色水滴）
  + 花体站名 + 副标题，配霞鹜文楷 / Maple Mono 同款字体
- `og:image` 指向自定义域名绝对地址 `https://b.quexing.cc.cd/_media/og-card.png`
- 站点已绑定自定义域名 b.quexing.cc.cd（仓库设置中配置，docs/ 下暂缺 CNAME 文件）
- 对应优化清单 SEO 第三条已勾选

## 2026-07-29

### 优化：提升辅助文字对比度，统一暗色卡片色调

- 文件：`docs/css/theme.css`
- 文章日期：亮色 `#aaa` → `#737373`，暗色 `#71717a` → `#a1a1aa`（对比度从约 2.3:1 提升到 4.5:1 以上，小字号日期不再费眼）
- 暗色 Hero 副标题：`#71717a` → `#a1a1aa`（与日期同色，一并修复对比度不足）
- 暗色文章卡片背景：`#252540` → `#2d2d31`（原色偏蓝紫，与 `#353537` / `#404042` 的中性灰背景脱节，改为灰紫色后整体更统一）

### 修复：暗色模式切换后代码高亮丢失

- 文件：`docs/js/darkmode.js`
- 原因：Shiki 高亮后 `<code>` 的 class 从 docsify 的 `lang-xxx` 变为 `language-xxx`，切换主题重新高亮时只识别 `lang-` 前缀，导致所有代码块降级为纯文本高亮
- 修复：语言识别兼容 `lang-` / `language-` 两种前缀；shiki 改为循环外单次 import；`Promise.all` 并行高亮；复制按钮最后统一添加一次

### 优化：CDN 依赖全部锁定精确版本

- 文件：`docs/index.html`、`docs/js/darkmode.js`
- 共 20 条 URL，逐条验证可访问（HTTP 200）
- 原则：锁定到当前实际运行的版本，不升级任何主版本
- 锁定清单：

| 包 | 原写法 | 锁定为 |
| --- | --- | --- |
| docsify（含 zoom-image、search 插件） | `@4` / 无版本 | `@4.13.1` |
| katex | `@latest` | `@0.18.1` |
| marked | `@4` | `@4.3.0` |
| mermaid | `@10` | `@10.9.6` |
| shiki | `@1` | `@1.29.2` |
| docsify-themeable | `@0` | `@0.9.0` |
| docsify-sidebar-collapse | 无版本 | `@1.3.5` |
| docsify-pagination | 无版本 | `@2.10.1` |
| docsify-count | `@latest` | `@1.1.0` |
| docsify-updated | 无版本 | `@1.0.5` |
| docsify-footnote | 无版本 | `@1.0.8` |
| Maple Mono CN 字体 | 无版本 | `@7.9.0` |
| click-heart-animation | `@main` 分支 | `@v1.0.0` tag |

- 顺带把 unpkg 的 docsify-mermaid 统一到 jsdelivr（随后因懒加载改造被移除）

### 优化：KaTeX / Mermaid / Shiki 改为按需懒加载

- 文件：`docs/index.html`（新增「重型库懒加载」脚本块，含详细注释）、`docs/js/darkmode.js`
- **KaTeX（≈270KB）**：新增 `lazyKatexPlugin`，markdown 解析前探测 `$...$` / `$$...$$`，没有公式不加载；渲染逻辑自实现于 `renderMath`（替代 docsify-katex，因其解析阶段同步依赖 `window.katex`，无法懒加载）；渲染时跳过代码块/行内代码，防止代码里的 `$` 被误判
- **Mermaid（≈2.7MB）**：新增 `lazyMermaidPlugin`，文章渲染后扫描 ` ```mermaid ` 代码块，有才动态 import 并渲染（替代 docsify-mermaid 与顶层 import）；插件顺序排在 Shiki 之前，防止 mermaid 块被误高亮
- **Shiki**：顶层 import 改为 `window.loadShiki` 懒加载器（Promise 缓存，全站只下载一次），`darkmode.js` 切换主题时复用同一加载器，版本 URL 单点维护
- 移除：katex 全局 css/js、docsify-katex、docsify-mermaid、mermaid 顶层 import、`mermaidConfig` 旧配置
- 效果：首页及无代码/公式/图的文章页减少约 3MB 下载，首屏明显变快
- 验证：全部内联脚本语法检查通过，无旧引用残留
