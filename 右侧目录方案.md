# 右侧目录（文章标题导航）方案

> 阅读长文章时在右侧显示本文标题目录，支持点击跳转、滚动高亮、展开/收起。
> 本文档记录**当前生效的设计**与维护要点。`docs/js/toc.js` 顶部注释指向本文件。

## 背景结论

- docsify v5 渲染标题时自动生成锚点（`<h2 id="xxx">`），点击跳转的基础能力是现成的。
- 社区插件 docsify-plugin-toc 最后更新于 2023-01（早于 docsify v5 两年），只带亮色样式；
  本站升级 v5 时已吃过社区插件不兼容的亏（sidebar-collapse → 自研 `js/sidebar.js`），故自研。
- 文章结构惯例：`# h1` = 文章标题（每篇 1 个），`h2`/`h3`/`h4` = 小节。
  目录收录 **h2–h4，不收 h1**。
- 目录扫描的是**渲染后的 DOM**，所以知识库长文里代码块内的 `# SQL 注释` 不会被误收
  （此时已是 `<pre>`）。

## 涉及文件

| 文件 | 作用 |
|---|---|
| `docs/js/toc.js` | 目录核心逻辑：收集标题、生成列表、胶囊切换、状态记忆、点击跳转、滚动高亮（含点击锁定）、手机端面板 |
| `docs/css/toc.css` | 目录布局与样式、正文让位过渡、窄屏 FAB 与底部面板、全站隐藏滚动条 |
| `docs/js/reading-progress.js` | 顶部进度条 + 右侧滚动滑块（与目录同属右侧 UI，样式在 `theme.css`） |
| `docs/index.html` | 引入以上文件（`<link>` / `<script>` 各一行） |

## 桌面端（≥1280px）

- **双胶囊切换**：收起时右上角显示「目录」小胶囊（64×40），展开时缩放切换为
  「收起目录」大胶囊（120×40）。两胶囊中心距右缘均为 110px，切换时视觉中心不动。
- **正文让位**：展开时 `body.toc-open` 给 `.content` 加 `--toc-indent`（260px =
  列表 220px + 间距 40px）右内边距，正文（宽 75% 居中）平滑变窄（250ms）；收起时归零。
  任何窗口宽度都不会与目录重叠。
- **目录列表**：220px 宽、贴页面右缘（`right: 0`）、无背景框/圆角/阴影，
  左侧 1px 分隔线；`max-height: calc(100vh - 140px)`，过长时内部滚动
  （滚动条本身也隐藏，靠滚轮/触摸）。
- **状态记忆**：localStorage 键 `qx-toc-open`，默认展开；只记「是否想看目录」，与具体页面无关。

## 手机端（<1280px）

- 右栏与右侧滑块隐藏，改为右下角「目录」浮动按钮（FAB，高 40px 圆角胶囊，主题色），
  底部留出 `env(safe-area-inset-bottom)` 避开 iPhone 横条。
- 点开底部弹出面板（`max-height: 65vh`，顶部圆角 12px，半透明遮罩），面板打开时锁住背景滚动。
- 面板内目录列表自带 3px 主题色滑块（滚动时出现，停滚 1.2 秒后隐藏）。
- 点标题项关闭面板并跳转；点遮罩 / ✕ / Esc 同样关闭。

## 行为细节

- **显示范围**：除首页外所有页面都显示目录（2026-08-28 起取消「标题少于 3 个则隐藏」的门槛）；
  首页无目录。标题为 0 个时列表为空，仅保留胶囊。
- **重建时机**：初次加载、切换文章、搜索跳转后都会自动重建（docsify `doneEach` 插件机制）。
- **滚动高亮**：高亮「当前正在读」的小节，判定线为标题顶部越过距视口顶 120px；
  页面滚到底时强制高亮最后一项；目录自身过长时会把高亮项滚进可视区。
- **点击锁定**：点击目录项后高亮锁定在被点项，平滑滚动途中不漂移；
  用户主动滚动（滚轮/触摸/方向键等）后解锁，另有 5 秒兜底解锁。

## 进度条与滚动滑块

两套都由 `docs/js/reading-progress.js` 创建，样式在 `docs/css/theme.css`：

- **顶部常驻横条**（`.reading-progress`）：始终可见，宽度随滚动进度增长。
- **右侧滚动滑块**（`.scroll-indicator`）：3px 宽主题色，滚动时淡入（opacity 0.5），
  停止 1.2 秒后淡出；桌面端显示，**窄屏隐藏**（见 `toc.css` 的 `@media (max-width: 1279px)`）。

## 维护备注

- **正文让位公式**：`body.toc-open .content { padding-right: var(--toc-indent) }`。
  若调整列表宽度（`.page-toc-list` 的 220px），`--toc-indent` 需同步按「列表宽 + 40px」调整；
  若只改 `--content-max-width`（正文 75%）或侧栏宽度，无需动目录代码。
- **胶囊定位**：小胶囊在 `.page-toc` 容器内（`top: 64px; right: 40px`，容器宽 140px 居中），
  大胶囊与列表为 `position: fixed`（大胶囊 `top: 64px; right: 50px`，列表 `top: 112px; right: 0`）。
- **目录项选择器权重**：必须压过 docsify v5 core 主题的 `button[type]`（0,1,1）及其 hover
  规则（最高 0,2,1），否则目录项会被渲染成灰底胶囊按钮——故用三层 class
  （`.page-toc .page-toc-list .page-toc-item`）。
- **插件注册方式**：`js/toc.js` 在 body 底部自行 `push` 进 `window.$docsify.plugins`。
  可行原因是 docsify v5 到 `DOMContentLoaded` 才读取配置初始化。若升级 docsify 大版本，
  需重新验证这一点。
- **fixed 定位与滚动条**：`position: fixed` 按「滚动条左侧的可视区」定位，与正文布局的宽度基准
  （含滚动条）差一个滚动条宽度（Windows 经典滚动条约 15px）。做贴边元素时应留出余量，
  或像本方案一样让正文主动让位。本站已全站隐藏浏览器滚动条（`toc.css` 的
  `html { scrollbar-width: none }`），影响更小。

## 历史：方案 A（已回退）

早期曾用「悬浮目录」方案：目录浮在正文右侧空白处、正文布局不动、仅 ≥1550px 宽屏显示。
2026-08-27 实测确认失败——Windows 经典滚动条宽约 15px，正文按含滚动条的宽度计算，而
`position: fixed` 的目录按去掉滚动条的可视区定位，被向左推 15px，恰好压住正文右缘 16px；
且用户希望目录独立成栏、正文让位、可自行控制显隐。

回退记录：`git revert 1d10a91`（生成提交 `1af8111`）。方案 B 由此而来。

## 如何彻底移除目录功能

方案 B 由多笔提交演进而来（样式微调较多），无法用单笔 `git revert` 干净撤销。若要移除：

1. 删除 `docs/js/toc.js`、`docs/css/toc.css`
2. 移除 `docs/index.html` 里对应的 `<link>` 与 `<script>` 各一行
3. 若同时不要进度条，再删 `docs/js/reading-progress.js` 及其引用（注意 `theme.css` 里的
   `.reading-progress` / `.scroll-indicator` 样式可一并清理）
