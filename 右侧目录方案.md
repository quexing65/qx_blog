# 右侧目录（文章标题导航）方案

> 目标：阅读长文章时，在右侧显示本文的标题目录，支持点击跳转、滚动高亮、手动展开/收起。
> 本文档记录两个方案的思路、方案 A 被回退的原因，以及用 git 在方案间切换的操作步骤。

## 背景评估结论（2026-08-27）

- docsify v5 渲染每个标题时自动生成跳转锚点（`<h2 id="xxx">`），点击跳转的基础能力是现成的。
- 社区插件 docsify-plugin-toc 最后更新于 2023-01（早于 docsify v5 两年），且只带亮色样式；本站升级 v5 时已吃过社区插件不兼容的亏（sidebar-collapse → 自研 js/sidebar.js），故选择自研。
- 本站文章结构惯例：`# h1` = 文章标题（每篇 1 个），`## h2` / `### h3` / `#### h4` = 小节 → 目录收录 h2–h4，不收 h1。
- 知识库长文（如 MySQL 笔记）里大量 `^# ` 是代码块内的 SQL 注释，不是真标题；目录扫描的是**渲染后的 DOM**（代码块已是 `<pre>`，里面的 `#` 不会被误收）。

## 方案 A：右侧悬浮目录（❌ 已实施 → 已回退）

**思路**：目录悬浮在正文右侧的空白处（正文宽 75% 居中，右侧天然空 12.5%），正文布局完全不动，只在 ≥1550px 宽屏显示。

**被回退的原因**（2026-08-27 实测确认）：

1. **压住了正文**。Windows 的经典滚动条宽约 15px：正文布局按含滚动条的宽度计算，而 `position: fixed` 的目录相对"去掉滚动条后的可视区"定位，被向左推了 15px——预留的空隙恰好被吃掉，实测目录左缘压住正文右缘 16px。
2. 即使没有滚动条问题，用户反馈希望**目录独立成栏、正文让位**，且能自己控制显隐，而不是只在大屏显示。

回退提交：`git revert 1d10a91`（生成提交 `1af8111`）。

## 方案 B：独立右栏 + 收缩键（✅ 当前实施）

**思路**：目录占据右侧独立栏位，**展开时正文自动变窄让位，收起时正文恢复全宽**；由页面右缘的收缩键（竖条按钮）控制，状态存 localStorage，刷新/换页/重开浏览器都记得住。这是 VuePress / Docusaurus 等现代文档站的通行布局。

**涉及文件**：

| 文件 | 作用 |
|---|---|
| `docs/js/toc.js` | 核心逻辑：收集标题、生成目录、收缩键交互、状态记忆、点击跳转、滚动高亮 |
| `docs/css/toc.css` | 布局（正文让位/恢复的过渡动画）、目录样式、收缩键样式、窄屏隐藏 |
| `docs/index.html` | 引入上面两个文件（一行 link + 一行 script） |

**行为细节**：

- **收缩键**：贴页面右缘、垂直居中的竖条按钮，展开时显示 `›`（点击收起），收起时显示 `‹`（点击展开）；配色复用左侧 docsify 侧栏开关的同款变量，亮暗色自动跟随。
- **正文让位**：展开时 `body.toc-open` 给 `.content` 加 260px 右内边距，正文（宽 75% 居中）随之变窄并平滑左移（250ms 过渡）；收起时归零、正文恢复全宽。1920px 屏幕上正文在 1020px（展开）↔ 1215px（收起）之间切换，目录与正文之间始终保持约 195px 空隙，**任何窗口宽度都不会重叠**（不再依赖"挤空白"的数学）。
- **状态记忆**：localStorage 键 `qx-toc-open`，默认展开；只记"是否想看目录"，与具体页面无关。
- 初次加载、切换文章、搜索跳转后都会自动重建目录（docsify 插件机制 `doneEach`）；标题少于 3 个的页面不显示目录和收缩键（首页因此自动跳过）。
- 滚动时高亮"当前正在读"的小节；页面滚到底时强制高亮最后一项；目录自身过长时内部滚动，且自动把高亮项滚进可视区。
- **窄屏（<1280px）**：目录与收缩键整体隐藏、正文不让位（此时放不下独立栏位）。

## 如何回退 / 切换方案（git 操作）

当前历史：`8ad5edb`（本文档）→ `1d10a91`（方案A）→ `1af8111`（回退A）→ **方案B（最新）**。
方案 B 是一笔独立提交，全程用 `revert` 撤销，不改写历史，推送不会冲突：

```bash
# 1. 查看提交历史，找到方案B那笔提交，复制提交号（前 7 位）
git log --oneline

# 2. 撤销方案B（自动删掉 toc.js/toc.css 并还原 index.html，回到没有右侧目录的状态）
git revert <方案B的提交号>
#   若方案B恰好是最后一笔提交，直接： git revert HEAD

# 3. 推送，Vercel 自动重新部署
git push
```

若想回到方案 A 的悬浮式（不推荐，见上文回退原因）：`git revert <方案B提交号>` 后再 `git revert 1af8111`（把"回退A"再回退掉）。

## 维护备注

- **正文让位的实现**：`body.toc-open .content { padding-right: var(--toc-indent) }`。若调整目录宽度 `--toc-width`，让位宽度 `--toc-indent` 按公式（目录宽 + 40px）自动跟随；若调整 `--content-max-width`（正文 75%）或侧栏宽度，无需改目录代码。
- **目录项/收缩键选择器权重**：必须压过 docsify v5 core 主题的 `button[type]`（0,1,1）及其 hover 规则（最高 0,2,1），否则目录项会被主题渲染成灰底胶囊按钮——目录项用三层 class（`.page-toc .page-toc-list .page-toc-item`），收缩键用 `body > button.toc-toggle`。
- **插件注册方式**：`js/toc.js` 在 body 底部自行 `push` 进 `window.$docsify.plugins`。可行原因是 docsify v5 到 `DOMContentLoaded` 才读取配置初始化（源码 `documentReady(() => new Docsify)`）。若未来升级 docsify 大版本，需重新验证这一点。
- **fixed 定位与滚动条**：`position: fixed` 的元素按"滚动条左侧的可视区"定位，与正文布局的宽度基准（含滚动条）不一致，差一个滚动条宽度（Windows 经典滚动条约 15px）。做贴边元素时不要按"正文空隙"精确计算，应留出滚动条宽度以上的余量，或像方案 B 一样让正文主动让位。
