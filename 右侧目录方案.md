# 右侧目录（文章标题导航）方案

> 目标：阅读长文章时，在正文右侧显示本文的标题目录，支持点击跳转、滚动时高亮当前小节。
> 本文档记录两个实施方案的思路，以及"方案 A 做得不满意时如何用 git 回退并换方案 B"的操作步骤。

## 背景评估结论（2026-08-27）

- docsify v5 渲染每个标题时自动生成跳转锚点（`<h2 id="xxx">`），点击跳转的基础能力是现成的，目录插件只需"收集标题 → 列在右边 → 点击滚动过去"。
- 本站正文区宽度为 75%（`--content-max-width`，见 theme.css）且居中，右侧天然空出约 12.5% 的内容区宽度，物理上有位置放目录。
- 社区插件 docsify-plugin-toc 最后更新于 2023-01（早于 docsify v5 两年），且只带亮色样式；本站升级 v5 时已吃过社区插件不兼容的亏（sidebar-collapse → 自研 js/sidebar.js），故选择自研。
- 本站文章结构惯例：`# h1` = 文章标题（每篇 1 个），`## h2` / `### h3` / `#### h4` = 小节 → 目录收录 h2–h4，不收 h1。
- 知识库长文（如 MySQL 笔记）里大量 `^# ` 是代码块内的 SQL 注释，不是真标题；目录扫描的是**渲染后的 DOM**（代码块已是 `<pre>`，里面的 `#` 不会被误收）。

## 方案 A：右侧悬浮目录（✅ 当前实施）

**思路**：目录悬浮在正文右侧的空白处，正文布局完全不动。只在足够宽的屏幕（≥1550px）显示，窄屏自动隐藏。优点是零风险、随时可整体撤销；缺点是 1550px 以下的屏幕看不到目录。

**涉及文件**：

| 文件 | 作用 |
|---|---|
| `docs/js/toc.js` | 核心逻辑：收集标题、生成目录、点击跳转、滚动高亮 |
| `docs/css/toc.css` | 目录样式：定位/宽度、亮暗色跟随主题、窄屏隐藏 |
| `docs/index.html` | 引入上面两个文件（一行 link + 一行 script） |

**行为细节**：

- 初次加载、切换文章、搜索跳转后都会自动重建目录（挂接 docsify 插件机制 `doneEach`）。
- 标题少于 3 个的页面不显示目录（首页因此自动跳过）。
- 滚动时高亮"当前正在读"的小节；页面滚到底时强制高亮最后一项；目录自身过长时内部滚动，且自动把高亮项滚进可视区。
- 亮/暗色自动跟随主题（颜色全部引用现有 CSS 变量，无两套配色）。
- 目录宽度公式 `min(220px, 右侧空白宽度)` 依据：正文宽 75% → 两侧各空 12.5%。**若以后调整 `--content-max-width` 或侧栏宽度，需同步改 toc.css 里的宽度公式**（有注释标注位置）。

## 方案 B：固定右栏布局（⏳ 备用，未实施）

**与 A 的区别**：A 是"悬浮在空白处"，屏幕不够宽就整个消失；B 把目录做成布局的一部分（类似 VuePress / Docusaurus 的文档站），正文略微左移让位，约 1200px 起目录就可见，观感更"现代文档站"，但要动正文布局，改动面更大。

**改动范围**（约在 A 的基础上改 2 个文件）：

1. `css/toc.css`：`.page-toc` 保持 fixed 定位，但宽度改为固定列宽（如 200px），响应式断点从 1550px 降到约 1200px。
2. `css/theme.css`：给 `.markdown-section` 让出右侧空间——把 `--content-max-width` 从 75% 调小（如 68%），或在宽屏媒体查询里加 `margin-right`。**这一步是 B 与 A 的本质区别：正文让位 vs 悬浮空白**。
3. `js/toc.js` 逻辑基本不用动（收集/跳转/高亮完全一致）。

## 如何回退方案 A / 切换到方案 B（git 操作）

方案 A 是**一笔独立提交**（提交信息含"方案A"字样），文档是它前面的一笔。全程用 `revert`，不改写历史，推送不会冲突，对新手最安全。

```bash
# 1. 查看提交历史，找到方案A那笔提交，复制它的提交号（前 7 位即可）
git log --oneline

# 2. 撤销方案A（会生成一笔新的"反向提交"，自动删掉 toc.js/toc.css 并还原 index.html）
git revert <方案A的提交号>
#   若方案A恰好是最后一笔提交，直接： git revert HEAD

# 3. 推送，Vercel 自动重新部署，线上即回到没有目录的状态
git push
```

撤销后即可实施方案 B（改法见上文"改动范围"，可再让 ZCode 按本文档实施）。

## 维护备注

- 目录插件注册方式：`js/toc.js` 在页面底部自行 `push` 进 `window.$docsify.plugins`。可行原因是 docsify v5 到 `DOMContentLoaded` 才读取配置初始化（源码 `documentReady(() => new Docsify)`），而底部脚本先于该时机执行。若未来升级 docsify 大版本，需重新验证这一点。
- `index.html` 的 plugins 数组是顺序敏感的（front-matter、mermaid/shiki 有先后依赖），目录插件与顺序无关，所以没放进数组、放在文件底部，并在数组注释里有说明。
