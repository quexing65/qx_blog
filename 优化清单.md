# 优化清单

> 2026-07-29 全站代码审查后待办的优化项，按优先级排列。
> 已完成的改动见 `更新日志.md`。
> 2026-08-17 更新：已完成的 4 项（SEO 基础两项、死代码清理、PNG 压缩）已从清单移除。

## 🔍 SEO 基础（建议优先做）

- [x] 补 OG 标签（`og:title` / `og:description` / `og:image`），分享到微信/QQ/X 时有预览卡片
  （2026-08-17 已加 5 个 OG 标签 + 生成 1200×630 封面图 `docs/_media/og-card.png`，封面使用站点吉祥物与花体站名）
- [ ] 补 `robots.txt` 和 `sitemap.xml`，引导爬虫收录

**已知局限**：Docsify 是 SPA，正文靠 JS 在浏览器渲染，爬虫拿到的只有空 `<div id="app">`。
做完上面 2 项只能保证**首页**在搜索结果里是正常条目；**文章页**想被收录需做预渲染
（构建时把 markdown 生成静态 HTML），工作量大，个人博客可不做。

## 🧹 代码清理

- [ ] 确认 `docs/note/archive/` 归档文章的可见性是否符合预期：目前侧边栏不显示、
  全文搜索（`paths: "auto"`）也索引不到。如希望归档可搜索，需在 search 配置中显式添加路径

## 🛠 工程化

- [x] 补 `package.json`，把 `node scripts/update-sidebar.js`、`node scripts/check-spaces.js`、
  本地预览封装成 npm scripts（如 `npm run update` / `npm run serve`）
  （2026-08-17 已添加 `npm run update` / `npm run check` / `npm run serve`）
- [x] `check-spaces.js` 挂到 git pre-commit 钩子或 CI，防止带空格文件名入库
  （2026-08-18 CI 已上线：本地有 pre-commit 钩子，远端 push/PR 时 CI 同样跑 `npm run check`）
- [x] CI 自动运行 `update-sidebar.js`：push 新文章后自动更新侧边栏/首页并部署，免手动
  （2026-08-18 已上线 `.github/workflows/ci.yml`：跑 `npm run update` 后 `git diff --exit-code docs/`
  做 drift 校验，忘了跑 update 就 push 会直接报红；上线首日即抓到一个真实跨平台 bug——
  中文文件夹排序在 Windows/Linux 下不一致，已通过锁定 `zh-Hans-CN` locale 修复。
  附带新增 `npm run check:links` 外链可用性检查，2026-08-17 那类 CDN 静默失效从此进不了仓库）

## ✨ 功能增强（可选）

- [ ] 评论系统：giscus（基于 GitHub Discussions，免费、免后端）
- [ ] 回到顶部按钮、阅读进度条
- [ ] 访问统计：umami / 51.la 等
- [ ] RSS 订阅：通过 GitHub Actions 在构建时生成
