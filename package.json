{
  "name": "qx_blog",
  "private": true,
  "description": "quexing 的个人博客，基于 Docsify",
  "scripts": {
    "update": "node scripts/update-sidebar.js",
    "sync:vault": "node scripts/sync-vault.js",
    "check": "node scripts/check-spaces.js",
    "check:links": "node scripts/check-links.js",
    "test": "node --test scripts/*.test.js",
    "serve": "npx -y docsify-cli serve docs -p 3000"
  }
}
